HASŁA:
admin: admin
uczestnik samodzielny: 1234

## Development

Install dependencies with `npm install` (if not already present). The project
includes basic linting and test scripts.

Run ESLint:

```bash
npm run lint
```

Run Jest tests:

```bash
npm test
```

The current test suite contains a placeholder test to verify that Jest is
configured correctly.
